{
  "name": "arena-z-nft-transfer",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network arena-z",
    "deploy-simple": "hardhat run scripts/deploySimpleBatch.js --network arena-z",
    "start": "node server.js"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.5.0",
    "express": "^4.18.2"
  }
}