{
  "name": "arena-z-nft-transfer-web",
  "version": "1.0.0",
  "description": "Web interface for bulk NFT transfers on Arena-Z Chain",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
